import { BeanStub } from "ag-grid-community/dist/lib/context/beanStub";
import { IMenuFactory } from 'ag-grid-community/dist/lib/interfaces/iMenuFactory';
import { Column } from 'ag-grid-community/dist/lib/entities/column';
import { ContainerType } from 'ag-grid-community/dist/lib/interfaces/iAfterGuiAttachedParams';
export declare class StandardMenuFactory extends BeanStub implements IMenuFactory {
    private filterManager;
    private popupService;
    private focusService;
    private ctrlsService;
    private hidePopup;
    private tabListener;
    hideActiveMenu(): void;
    showMenuAfterMouseEvent(column: Column, mouseEvent: MouseEvent | Touch): void;
    showMenuAfterButtonClick(column: Column, eventSource: HTMLElement, containerType: ContainerType): void;
    private showPopup;
    private trapFocusWithin;
    isMenuEnabled(column: Column): boolean;
}
