import { AgEvent, ColumnEvent } from "ag-grid-community/dist/lib/events";
import { Column } from "ag-grid-community/dist/lib/entities/column";
import { BeanStub } from "ag-grid-community/dist/lib/context/beanStub";
export declare class AlignedGridsService extends BeanStub {
    private columnModel;
    private ctrlsService;
    private logger;
    private consuming;
    private setBeans;
    private init;
    private fireEvent;
    private onEvent;
    private fireColumnEvent;
    private fireScrollEvent;
    private onScrollEvent;
    getMasterColumns(event: ColumnEvent): Column[];
    getColumnIds(event: ColumnEvent): string[];
    onColumnEvent(event: AgEvent): void;
    private processGroupOpenedEvent;
    private processColumnEvent;
}
